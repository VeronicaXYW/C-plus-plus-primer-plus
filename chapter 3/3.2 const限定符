const可以本段代码内创建常量。
建议：将自己定义的只读变量的首字母大写，和其他首字母小写的可读可写变量区分开
在使用const的同时说明常量的数据类型和数值。比如 const int Months = 12;
如果没有注明数值，那么该常量的值将是不确定。即使在定义后的下一条语句就给常量赋值，也有可能无效。

const比define的优势，如下图：
const可以指定常量的类型；
const的作用域可以调整，放在函数体里就只能在该函数体里生效；
const可用于更复杂的类型，比如数组和结构。
